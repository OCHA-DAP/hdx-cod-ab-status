{
  "name": "my-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "fetch": "python3 scripts/m49.py && python3 scripts/gis.py && python3 scripts/plans.py && python3 scripts/regions.py && python3 scripts/offices.py && python3 scripts/cod_metadata.py && python3 scripts/hdx.py",
    "fetch:local": "uv run scripts/m49.py && uv run scripts/gis.py && uv run scripts/plans.py && uv run scripts/regions.py && uv run scripts/offices.py && uv run scripts/cod_metadata.py && uv run scripts/hdx.py",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write .",
    "update": "npx npm-check-updates -u && npm install",
    "sync": "uv run scripts/sync_data.py"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@eslint/compat": "^2.0.5",
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.5.2",
    "@types/papaparse": "^5.5.2",
    "astro": "^6.1.5",
    "eslint": "^10.2.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-astro": "^1.7.0",
    "globals": "^17.5.0",
    "prettier": "^3.8.2",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.58.1"
  },
  "dependencies": {
    "papaparse": "^5.5.3"
  }
}
